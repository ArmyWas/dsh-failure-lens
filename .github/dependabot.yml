version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
    groups:
      harness-client:
        applies-to: version-updates
        patterns:
          - '@deepseek-ai/*'
        # Keep the coupled Harness release train in one compatibility review.
        # Dependabot classifies rc.N -> rc.N+1 as a prerelease update rather
        # than `minor` or `patch`, so filtering update-types splits the group.
      development-tooling:
        applies-to: version-updates
        dependency-type: development
        exclude-patterns:
          - '@deepseek-ai/*'
        update-types:
          - minor
          - patch
    # Major upgrades need an explicit compatibility pass against the Harness
    # client peer graph. Security updates are not affected by this rule.
    ignore:
      - dependency-name: '*'
        update-types:
          - version-update:semver-major
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
